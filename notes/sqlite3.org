#+title: Sqlite3
#+filetags: personal sql

Examiner les tables et lister l'une d'entre elleS

#+begin_src sql
sqlite3 test.db
sqlite> .tables
sqlite> .schema files
sqlite> select * from files;
#+end_src
